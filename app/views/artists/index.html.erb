<h1>Artists</h1>
<table>
  <th class="col-large">
    <h3><tr align="left">
      <td>Name</td>
    </tr></h3>
  </th>
<% @artists.each do |artist| %>
  <tr>
    <td><%= link_to artist.name, artist_path(artist) %></td>
  </tr>
<% end %>
</table>
